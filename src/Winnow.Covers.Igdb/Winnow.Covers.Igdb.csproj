<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Winnow.Covers.Igdb</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Http" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.11" />
  </ItemGroup>

  <ItemGroup>
    <!--
      The whole reason this project exists. Winnow.Covers is deliberately
      source-agnostic — it knows about ICoverSource and nothing about who
      implements one — so the dependency on the IGDB client lives out here,
      where only the composition root ever sees it. Winnow.Covers must never
      reference Winnow.Enrich.Igdb; if it did, every consumer of the cover cache
      would inherit a credential-shaped dependency it has no use for.
    -->
    <ProjectReference Include="..\Winnow.Covers\Winnow.Covers.csproj" />
    <ProjectReference Include="..\Winnow.Enrich.Igdb\Winnow.Enrich.Igdb.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Winnow.Covers.Tests" />
  </ItemGroup>

</Project>
