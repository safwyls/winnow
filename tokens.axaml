<!--
    Design tokens — Game Library Manager

    ⚠ THIS FILE IS THE DESIGN REFERENCE, NOT THE BUILD INPUT.
    The authoritative, compiling copy is src/Hoard.App/Themes/tokens.axaml.
    It diverges here deliberately, for reasons recorded as TODOs in that file:
      · the Styles block is keyed (a ResourceDictionary cannot hold an unkeyed
        child) and promoted into Application.Styles at startup by App.axaml.cs
      · TimeSpan needs an explicit xmlns:sys="using:System"
      · a nested xmlns on <Styles> is rejected by the XAML compiler
      · the FontFeatures "wdth" setters were removed — Avalonia 11 has no
        variable-axis API, and Bricolage's own wdth axis tops out at 100
      · TileScrim was retuned to the §5.3 gradient
      · the Fluent ScrollBar key overrides are omitted here. They are not
        design tokens: they are the names Avalonia's own theme happens to read,
        and they carry values already stated below (Well, TextDim and two steps
        between Line and TextDim). Recording them twice would only give them
        somewhere to drift.
    Change tokens THERE. Edit this file only to keep the design record honest.

    ⚠ THESE VALUES ARE THE DEFAULT THEME, AND THEY ARE THE STARTING STATE.
    src/Hoard.App/Themes/HoardThemes.cs holds four palettes and ThemeService
    writes the chosen one onto these brushes at runtime — so what is declared
    here is "Hoard, at transparency zero", which is the state that is correct on
    every host. The other three (Nightshift, Tungsten, Box art) are not variants
    of this table and do not differ from it by hue: each takes a different
    TEMPERATURE, CHROMA STRATEGY, VALUE STRUCTURE and MATERIAL, which is what
    makes four themes rather than four settings of one. What they share is the
    grammar — Volt is the room at full voltage, and Flare is the one hue the room
    cannot produce, spent on unread updates and nothing else.

    Merge into App.axaml:

        <Application.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceInclude Source="avares://Hoard/Themes/tokens.axaml"/>
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Application.Resources>

    Fonts must be bundled as AvaloniaResource. All three are SIL OFL.
    Do not fall back to system fonts — the display face is load-bearing.
-->
<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ══ Colour ══════════════════════════════════════════════════════════
         The neutral family is one dark green-teal ink, stepped five times
         (design-system.md §2). It replaced the indigo-violet family in the
         palette revision recorded there: violet made teal look like a tint of
         the chrome and hot pink like a second brand colour, and the whole set
         read as the default dark-app purple.

         Flare is the rarest colour in the app. It marks ONLY unread updates.
         The moment it becomes a generic accent, the badge stops meaning
         anything and the product loses its point.
    -->
    <!-- ⚠ EVERY THEMEABLE BRUSH IS DECLARED Color="#RRGGBB" AS AN ATTRIBUTE,
         never as element content. The two forms look identical and are not:
         Avalonia's XAML compiler constant-folds

             <SolidColorBrush x:Key="X">#16282A</SolidColorBrush>

         into an ImmutableSolidColorBrush, whose Color cannot be written. The
         attribute form

             <SolidColorBrush x:Key="X" Color="#16282A"/>

         stays a real SolidColorBrush. ThemeService changes themes by writing
         Color on the brush objects the views already resolved (StaticResource
         looks up once and never again), so a folded brush is a token the theme
         system silently cannot reach — the window half repaints and the
         leftovers look like a rendering fault rather than a bug. Measured, not
         assumed: the first build of the theme system had thirty-five of them.
    -->
    <Color x:Key="WellColor">#050D0E</Color>
    <Color x:Key="GroundColor">#0F1C1E</Color>
    <Color x:Key="SurfaceColor">#16282A</Color>
    <Color x:Key="SurfaceRaisedColor">#1D3437</Color>
    <Color x:Key="LineColor">#2B4A4C</Color>
    <Color x:Key="TextColor">#F0EDE7</Color>
    <Color x:Key="TextDimColor">#8FA5A0</Color>
    <Color x:Key="FlareColor">#FF4D93</Color>
    <Color x:Key="VoltColor">#4DE8C2</Color>
    <Color x:Key="AmberColor">#FFB63D</Color>
    <Color x:Key="AzureColor">#57A8F0</Color>
    <Color x:Key="DangerColor">#E04B45</Color>

    <SolidColorBrush x:Key="Well"          Color="{StaticResource WellColor}"/>
    <SolidColorBrush x:Key="Ground"        Color="{StaticResource GroundColor}"/>
    <SolidColorBrush x:Key="Surface"       Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SurfaceRaised" Color="{StaticResource SurfaceRaisedColor}"/>
    <SolidColorBrush x:Key="Line"          Color="{StaticResource LineColor}"/>
    <SolidColorBrush x:Key="Text"          Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="TextDim"       Color="{StaticResource TextDimColor}"/>
    <SolidColorBrush x:Key="Flare"         Color="{StaticResource FlareColor}"/>
    <SolidColorBrush x:Key="Volt"          Color="{StaticResource VoltColor}"/>
    <SolidColorBrush x:Key="Amber"         Color="{StaticResource AmberColor}"/>
    <SolidColorBrush x:Key="Azure"         Color="{StaticResource AzureColor}"/>
    <!-- Destructive affordances only; today that is the window close button's
         hover fill and nothing else. -->
    <SolidColorBrush x:Key="Danger"        Color="{StaticResource DangerColor}"/>

    <!-- Text and states for controls filled with Volt (Play, "Same game"). -->
    <SolidColorBrush x:Key="VoltInk" Color="#0C2A24"/>
    <SolidColorBrush x:Key="VoltHover" Color="#6FEDCE"/>
    <SolidColorBrush x:Key="VoltPress" Color="#3BD1AC"/>

    <!-- ══ The rest of the family, named ══════════════════════════════════
         These were literal hexes scattered through six views until the theme
         system needed to reach them. A colour written inline is a colour no
         theme can change, and the failure is silent: the window half repaints
         and the leftovers look like a rendering fault. Every one of them is
         "some role at N%" or "one step past a step", so they are DERIVED from
         the palette in HoardTheme.Tokens rather than restated per theme. -->

    <!-- One step above SurfaceRaised: a selected menu item, a pressed caption
         button. SurfaceRaised on SurfaceRaised is no step at all. -->
    <SolidColorBrush x:Key="SurfaceHigh" Color="#254042"/>
    <!-- The quietest ink that is still ink: watermarks, disabled arrows. -->
    <SolidColorBrush x:Key="TextFaint" Color="#5A8286"/>

    <SolidColorBrush x:Key="DangerHover" Color="#EF645E"/>
    <SolidColorBrush x:Key="DangerPress" Color="#B33A35"/>
    <SolidColorBrush x:Key="DangerInk" Color="#FFF2EF"/>

    <!-- Volt at 30 / 24 / 40 %: text selection, a soft selection wash, a
         half-lit edge. Never a new colour, always the same one turned down. -->
    <SolidColorBrush x:Key="VoltSelection" Color="#4D4DE8C2"/>
    <SolidColorBrush x:Key="VoltSelectionSoft" Color="#3D4DE8C2"/>
    <SolidColorBrush x:Key="VoltEdgeSoft" Color="#664DE8C2"/>

    <!-- Flare at 70 / 85 %. Still only ever on an unread marker (§5.2). -->
    <SolidColorBrush x:Key="FlareSoft" Color="#B3FF4D93"/>
    <SolidColorBrush x:Key="FlareGlow" Color="#D9FF4D93"/>

    <SolidColorBrush x:Key="LineSoft" Color="#992B4A4C"/>
    <SolidColorBrush x:Key="SurfaceRaisedHalf" Color="#801D3437"/>
    <SolidColorBrush x:Key="SurfaceRaisedGhost" Color="#1F1D3437"/>
    <SolidColorBrush x:Key="SurfaceRaisedFaint" Color="#141D3437"/>
    <SolidColorBrush x:Key="GroundVeil" Color="#4D0F1C1E"/>
    <!-- The detail modal's scrim (§9): Well, the tone one step below Ground.
         Since the caption took the rail's colour, this and the scrollbar track
         are the two surfaces that still want a tone under the art field. -->
    <SolidColorBrush x:Key="ModalScrim" Color="#D6050D0E"/>

    <!-- Hover overlay scrim on cover tiles -->
    <LinearGradientBrush x:Key="TileScrim" StartPoint="0%,40%" EndPoint="0%,100%">
        <GradientStop Offset="0"   Color="#000F1C1E"/>
        <GradientStop Offset="0.55" Color="#B30F1C1E"/>
        <GradientStop Offset="1"   Color="#EB0F1C1E"/>
    </LinearGradientBrush>

    <!-- ══ Window chrome ══════════════════════════════════════════════════
         The app draws its own caption (ExtendClientAreaToDecorationsHint).

         THE CAPTION TAKES THE RAIL'S COLOUR. It used to take Well, one step
         below Ground — the "unlit lip". That rule bought the right thing (no
         bright platform strip above the art) by the wrong means: it made the
         chrome two tones meeting at a corner. Painted in the same Surface, the
         caption and the rail are one continuous bracket and the art is a
         recessed field inside it. See design-system.md §9, as amended.
    -->
    <x:Double x:Key="TitleBarHeight">36</x:Double>
    <x:Double x:Key="WindowButtonWidth">46</x:Double>

    <!-- ══ The five grounds ═══════════════════════════════════════════════
         Which surface may admit the desktop, and which may not. This replaces
         the caption-only Mica rule and resolves design-system.md §13 gap 7.

         The old rule was reached by a correct measurement and a wrong
         conclusion. TextDim on SurfaceRaised at 85% over white is 3.1:1, under
         AA — so translucency was confined to the 36px caption, where it is
         effectively invisible. What that sum actually proves is narrower than
         it looked: it proves you cannot take an ink CHOSEN FOR AN OPAQUE GROUND
         and subtract alpha from it. So the chrome takes a DARKER ink and a
         BRIGHTER dim ink as it opens up. TRANSPARENCY IS A QUANTITY, NOT A
         SWITCH: the Appearance screen sets it with a slider, 0 (fully opaque,
         the default) to 100, and every token below walks continuously between
         its opaque value and the theme's translucent ink. The inks finish that
         walk in the first quarter of the track and the alpha keeps falling for
         the other three, which is what buys the range that still clears AA
         against a white wallpaper. HoardTheme carries the endpoints;
         ThemeContrastTests carries the proof, across the range.

           ShellGround   the whole client area below the caption. Opaque Ground
                         at zero; NOTHING at all the moment any transparency is
                         asked for, because the columns over it paint their own
                         — which is what lets the rail be see-through without
                         the window painting an opaque field behind it first,
                         and why it is a step rather than a ramp: two stacked
                         alphas multiply, and the slider could never reach its
                         own end.
           WallGround    the field the covers hang in. OPAQUE UNLESS ASKED FOR,
                         and opaque at slider zero either way. It used to be
                         opaque at every setting on a §1 argument - the art is
                         the interface, and a wallpaper behind six hundred
                         capsules is a second image competing with all of them -
                         and that argument was overruled by the person looking
                         at the result: a solid slab bolted to translucent
                         chrome reads as two windows. The half of the reasoning
                         that was construction rather than taste still binds,
                         and it is answered by TileGround below rather than
                         here. When asked for, the field admits HALF the desktop
                         the chrome does (MinWallAlpha 0.65 against the chrome's
                         0.30) - see HoardTheme for the two measurements behind
                         the ratio.
           PaneGround    the merge queue, the Stores and Appearance panes, the
                         library's LIST view and the empty state. THE WALL'S OWN
                         RAMP, and the wall's own setting. It used to be opaque
                         at every setting on the grounds that these are text
                         sitting directly on the field - true - and that §14.3
                         therefore rules them out. The principle was right and
                         the number was the wrong one: §14.3's measurement is
                         against the CHROME's reach, and the wall admits 0.35 of
                         the desktop against the chrome's 0.70. Walked per theme
                         against white, TextDim clears AA to 59/71/65/73 percent
                         of the slider on the open field, against the chrome's
                         own 27/31/30/26 - so a pane is not the surface that
                         fails first, it fails at more than twice the reach of
                         the one the app already carries labels on.
                         PaneGround is also what the LIBRARY PANE'S OWN BARS
                         sit on. The command bar and the cut bar are inside the
                         pane (design-system.md §15.1, revised) and paint no
                         fill of their own, so there is no ChromeGround token
                         any more: a second coat of the pane's ink at a second
                         alpha over the first is exactly the double composite
                         ShellGround is a step and not a ramp to avoid.
           ChromeRaised  a rail or panel row's hover and selection fill. Opaque,
                         it is the ordinary Surface -> SurfaceRaised step: an ink
                         that REPLACES what is under it. Translucent it has to be
                         a VEIL, because a darker ink over an already-translucent
                         rail composites downwards and the raised row comes out
                         darker than its neighbour. Only one veil is backdrop-
                         independent — solving a*(V-rail) = L*(Text-rail) for
                         every rail gives V = Text — so the veil IS Text, at a
                         strength that starts as the theme's own opaque step and
                         grows to 10%. Elevation stays relative, which is what §6
                         says it is.
           ChromeFieldOnGround / ChromeFieldOnSurface
                         an INPUT's fill - the search box and the action bar's
                         prompt inside the library pane, the filter panel's find
                         and year fields in the panel. A field is one step from
                         its container in the neutral family, in the direction
                         the opaque palette already took: the pane is Ground so
                         a field on it is Surface, the panel is Surface so a
                         field in it is Ground. Both take the WALKED ink.

                         A field is a CHILD of its container, so the two alphas
                         stack, and that fixes its own alpha outright rather
                         than leaving it to taste:

                             fieldAlpha = 1 - (1 - 0.65) / (1 - containerAlpha)

                         THE TWO FIELDS GET DIFFERENT ANSWERS, and that is the
                         identity working rather than an inconsistency. The
                         filter panel is chrome, so its container admits 0.70
                         and MinFieldAlpha is 0.50 - a field admits HALF of what
                         the surface around it admits, and TextDim in it clears
                         AA to 71-76% of the slider against the chrome's 26-31%.
                         The search box's container is the LIBRARY PANE, which
                         already admits 0.35 exactly; there is no half left to
                         spend, MinPaneFieldAlpha solves to ZERO, and past the
                         ink ramp that field paints nothing at all. It is drawn
                         by its Line border and lit by its Volt ring, which is
                         what drew it anyway - the fill and the surface around it
                         converge to 1.05:1 at the far end either way. At SOLID
                         it is opaque Surface, so the step it cuts is untouched.

                         It also follows the WALL's setting rather than the
                         slider's, because with the art field solid the pane
                         under it is solid, and there is nothing being admitted
                         for the field to match.
           TileGround    under the art stack inside a tile, so a cover that has
                         decoded one of its two dormancy layers and not the
                         other cannot show the window through the gap.

           CaptionFill   the 36px title lip. Always EXACTLY ChromeSurface —
                         same ink and same alpha, not merely a matching colour,
                         because a second alpha over the same backdrop lands on
                         a second tone and puts the corner back.

         Popovers do NOT open up: a flyout is its own popup root and never
         receives the window's backdrop, so a translucent fill there would
         sample the application rather than the desktop and give a different
         answer at every position. They keep SurfaceRaised, opaque, at every
         slider position.

         Every value here is written at runtime by ThemeService. What is
         declared is the OPAQUE state on the default theme, because that is the
         state that is correct on every host — a machine where none of this
         works needs nothing to go right in order to look deliberate. -->
    <SolidColorBrush x:Key="ShellGround" Color="#0F1C1E"/>
    <SolidColorBrush x:Key="CaptionFill" Color="#16282A"/>
    <SolidColorBrush x:Key="ChromeSurface" Color="#16282A"/>
    <SolidColorBrush x:Key="WallGround" Color="#0F1C1E"/>
    <SolidColorBrush x:Key="PaneGround" Color="#0F1C1E"/>
    <SolidColorBrush x:Key="ChromeRaised" Color="#1D3437"/>
    <!-- Half a step: a hovered row where ChromeRaised is a selected one. -->
    <SolidColorBrush x:Key="ChromeRaisedHalf" Color="#801D3437"/>
    <SolidColorBrush x:Key="ChromeFieldOnGround" Color="#16282A"/>
    <SolidColorBrush x:Key="ChromeFieldOnSurface" Color="#0F1C1E"/>
    <SolidColorBrush x:Key="TileGround" Color="#0F1C1E"/>

    <!-- ══ Dormancy ramp ══════════════════════════════════════════════════
         Consumed by the cover renderer. Clamped floor is deliberate: a cover
         you cannot identify is a cover you cannot choose. Colour-agnostic —
         untouched by the palette revision.
    -->
    <x:Double x:Key="DormancySatFloor">0.22</x:Double>
    <x:Double x:Key="DormancyBrightFloor">0.68</x:Double>
    <x:Double x:Key="DormancyHueDegrees">-6</x:Double>
    <x:Double x:Key="DormancyFullMonths">36</x:Double>
    <TimeSpan x:Key="HoverRestoreDuration">0:0:0.140</TimeSpan>

    <!-- ══ Type ═══════════════════════════════════════════════════════════ -->
    <FontFamily x:Key="DisplayFont">avares://Hoard/Assets/Fonts#Bricolage Grotesque</FontFamily>
    <FontFamily x:Key="BodyFont">avares://Hoard/Assets/Fonts#Plus Jakarta Sans</FontFamily>
    <FontFamily x:Key="DataFont">avares://Hoard/Assets/Fonts#IBM Plex Mono</FontFamily>

    <x:Double x:Key="DisplayLSize">22</x:Double>
    <x:Double x:Key="DisplaySSize">12</x:Double>
    <x:Double x:Key="BodyLSize">15</x:Double>
    <x:Double x:Key="BodySize">13</x:Double>
    <x:Double x:Key="LabelSize">11</x:Double>
    <x:Double x:Key="DataSize">12</x:Double>
    <x:Double x:Key="DataSSize">10</x:Double>

    <!-- ══ Spacing (4px base) ═════════════════════════════════════════════ -->
    <x:Double x:Key="Space1">4</x:Double>
    <x:Double x:Key="Space2">8</x:Double>
    <x:Double x:Key="Space3">12</x:Double>
    <x:Double x:Key="Space4">16</x:Double>
    <x:Double x:Key="Space6">24</x:Double>
    <x:Double x:Key="Space8">32</x:Double>
    <x:Double x:Key="Space12">48</x:Double>

    <!-- ══ Geometry ═══════════════════════════════════════════════════════ -->
    <x:Double x:Key="RailWidth">220</x:Double>
    <x:Double x:Key="CommandBarHeight">48</x:Double>
    <x:Double x:Key="TileWidth">148</x:Double>       <!-- 2:3, matches 600x900 capsule -->
    <x:Double x:Key="TileHeight">222</x:Double>
    <x:Double x:Key="TileGutter">16</x:Double>
    <x:Double x:Key="TileMinWidth">108</x:Double>    <!-- density slider bounds -->
    <x:Double x:Key="TileMaxWidth">200</x:Double>
    <x:Double x:Key="ListRowHeight">44</x:Double>
    <x:Double x:Key="BadgeSize">10</x:Double>
    <CornerRadius x:Key="RadiusTile">6</CornerRadius>
    <CornerRadius x:Key="RadiusControl">4</CornerRadius>
    <!-- The floating layout's pane corner (§15). ABOVE the tile's 6 and the
         control's 4, so the three radii rank by the size of the object they
         round: radius reads as a proportion of the corner it turns, and 6px on a
         750px-tall column is a chamfer rather than a round. -->
    <CornerRadius x:Key="RadiusPane">8</CornerRadius>
    <!-- The gap between one pane and the next, and between a pane and the window
         edge. §4's own spacing step, and exactly the width of the resize band
         §9.1 measures — so a pane inset by it has no control the OS can eat. Its
         HALF is what an interior edge takes, so a pane-to-pane gap and a
         pane-to-window gap come out the same. -->
    <x:Double x:Key="PaneGap">8</x:Double>
    <Thickness x:Key="SelectionEdge">2,0,0,0</Thickness>

    <!-- ══ Text styles ════════════════════════════════════════════════════ -->
    <Styles xmlns="https://github.com/avaloniaui">

        <Style Selector="TextBlock.display-l">
            <Setter Property="FontFamily" Value="{StaticResource DisplayFont}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="{StaticResource DisplayLSize}"/>
            <Setter Property="FontFeatures" Value="wdth 105"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
        </Style>

        <Style Selector="TextBlock.display-s">
            <Setter Property="FontFamily" Value="{StaticResource DisplayFont}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="{StaticResource DisplaySSize}"/>
            <Setter Property="FontFeatures" Value="wdth 110"/>
            <Setter Property="LetterSpacing" Value="0.72"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
        </Style>

        <Style Selector="TextBlock.body-l">
            <Setter Property="FontFamily" Value="{StaticResource BodyFont}"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="{StaticResource BodyLSize}"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
        </Style>

        <Style Selector="TextBlock.body">
            <Setter Property="FontFamily" Value="{StaticResource BodyFont}"/>
            <Setter Property="FontSize" Value="{StaticResource BodySize}"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
        </Style>

        <Style Selector="TextBlock.label">
            <Setter Property="FontFamily" Value="{StaticResource BodyFont}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="{StaticResource LabelSize}"/>
            <Setter Property="LetterSpacing" Value="0.44"/>
            <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
        </Style>

        <!-- Every number in the app. tnum is mandatory in list view. -->
        <Style Selector="TextBlock.data">
            <Setter Property="FontFamily" Value="{StaticResource DataFont}"/>
            <Setter Property="FontSize" Value="{StaticResource DataSize}"/>
            <Setter Property="FontFeatures" Value="tnum"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
        </Style>

        <Style Selector="TextBlock.data-s">
            <Setter Property="FontFamily" Value="{StaticResource DataFont}"/>
            <Setter Property="FontSize" Value="{StaticResource DataSSize}"/>
            <Setter Property="FontFeatures" Value="tnum"/>
            <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
        </Style>

        <!-- ══ Focus — accessibility floor, not optional ══════════════════ -->
        <Style Selector=":is(Control):focus-visible">
            <Setter Property="FocusAdorner">
                <FocusAdornerTemplate>
                    <Border BorderBrush="{StaticResource Volt}"
                            BorderThickness="2"
                            Margin="-2"
                            CornerRadius="{StaticResource RadiusControl}"/>
                </FocusAdornerTemplate>
            </Setter>
        </Style>

    </Styles>

</ResourceDictionary>
